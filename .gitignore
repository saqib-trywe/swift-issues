# macOS
.DS_Store

# Swift / SwiftPM
.build/
.swiftpm/
# Package.resolved is committed deliberately: this package ships executables
# (server, CLI, MCP), so the dependency graph is pinned rather than floating.
# Without it, CI and a developer machine can silently build different code.

# Xcode
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
build/
